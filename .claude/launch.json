{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "static-site",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-c", "import os,http.server,socketserver; p=int(os.environ.get('PORT','0')); h=socketserver.TCPServer(('',p), http.server.SimpleHTTPRequestHandler); print('serving on port', h.server_address[1], flush=True); h.serve_forever()"],
      "autoPort": true,
      "startPath": "/pages/game.html"
    }
  ]
}
